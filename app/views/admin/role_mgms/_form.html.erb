<%= form_for @role, :url => {:controller => "admin/role_mgms" ,:action => "#{@role.new_record? ? "create" : "update"}" } do |f| %>
    <% if @role.errors.any? %>
        <div id="error_explanation">
          <h2><%= pluralize(@role.errors.count, "error") %> prohibited this dept from being saved:</h2>

          <ul>
            <% @role.errors.full_messages.each do |msg| %>
                <li><%= msg %></li>
            <% end %>
          </ul>
        </div>
    <% end %>

    <div class="field">
      <%= f.label "角色名称：" %>
      <%= f.text_field :name %>
    </div>

    <div class="actions">
      <%= f.submit value="提交" %>
    </div>
<% end %>
